import SwiftUI

struct MeldZoneView: View {
    var body: some View {
        Text("Melds go here")
            .padding()
            .background(Color.yellow.opacity(0.2))
            .cornerRadius(8)
    }
} 